import React from 'react';

export function ToolRating({ toolId }: { toolId: string }) {
  // Completely removed Firebase ratings as requested.
  // Returning null to hide the rating component from tool pages.
  return null;
}
